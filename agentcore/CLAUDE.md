# Project Rules

- When cloning GitHub repositories, always use `git clone` via Bash tool (not WebFetch)
- When generating .understandignore, use sensible defaults and proceed automatically
- After completing the knowledge graph analysis, the system will automatically write to Neptune
